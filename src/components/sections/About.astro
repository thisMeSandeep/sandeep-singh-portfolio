---
---

<section id="about" class="border-b border-[var(--border)]">
  <div
    class="mx-auto max-w-[var(--max-w-wide)] px-6 md:px-8 py-24 grid grid-cols-1 md:grid-cols-[45fr_55fr] gap-12 md:gap-16 items-start"
  >
    <!-- Left column -->
    <div class="flex flex-col gap-5">
      <span
        class="text-xs uppercase tracking-[var(--tracking-wider)] text-[var(--text-3)]"
      >
        About
      </span>
      <h2
        class="font-serif text-3xl leading-tight tracking-tight text-[var(--text-1)]"
      >
        I care about the details most people never see.
      </h2>
    </div>

    <!-- Right column -->
    <div class="flex flex-col gap-5">
      <p class="text-base text-[var(--text-2)] leading-[var(--leading-relaxed)]">
        I've spent the last several years building products end to end — shaping
        the data model, wiring up the API, and obsessing over the last few
        pixels of the interface. I like working close to the whole stack because
        that's where the interesting trade-offs live.
      </p>
      <p class="text-base text-[var(--text-2)] leading-[var(--leading-relaxed)]">
        Outside of client work I build small tools, write about what I learn,
        and keep a soft spot for fast, quiet software that respects the people
        using it.
      </p>
      <a
        href="#contact"
        class="link-arrow group inline-flex w-fit items-center gap-1.5 text-[var(--accent)] hover:text-[var(--accent-hover)] text-base mt-1"
      >
        More about me
        <span
          class="transition-transform duration-200 group-hover:translate-x-1"
          aria-hidden="true">&rarr;</span
        >
      </a>
    </div>
  </div>
</section>
